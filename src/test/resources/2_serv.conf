flags: ${flags}  [
  "-XX:+UnlockExperimentalVMOptions"
]

system_properties {
  app {
    rootLogPath = /tmp/app
    o2oBufferSizeMb = 20
  }
  agrona.disable.bounds.checks = false
}
main: main.Run
serviceName: hide
