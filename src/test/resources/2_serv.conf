flags: ${flags}  [
  "-XX:+UnlockExperimentalVMOptions"
]

system_properties {
  dijkstra {
    rootLogPath = /var/log/dijkstra
    o2oBufferSizeMb = 20
  }
  agrona.disable.bounds.checks = false
}
main: main.Run
serviceName: hide
